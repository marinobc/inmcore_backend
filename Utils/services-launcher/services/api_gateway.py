from core.colors import BLUE

SERVICE = {
    "id":    "api-gateway",
    "label": "API Gateway",
    "type":  "maven",
    "color": BLUE,
    # "order" controls startup sequence (lower = earlier)
    "order": 1,
}
